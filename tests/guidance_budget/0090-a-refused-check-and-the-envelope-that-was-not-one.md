# 0090 - a refused check, and the envelope that was not one

follows: 0089
follows-resident-measured: 106_973
follows-total-measured: 493_699
resident-ceiling: 108_000
resident-measured: 107_369
total-ceiling: 499_000
total-measured: 498_251

The rule against grading with a scorer that runs what the model wrote was carried by prose and nothing else, and the helper that does the running asked only whether importing was intended. It now asks the same reading of a file that the static stage already reports through, and refuses on a construct that reaches an engine, naming the file and the line. It asks it of two files rather than one: the scoring adapter, and the small module that reshapes a reply before scoring, which the same child imports and calls on every probe and which nothing had ever looked at, though the very flag that permits the import names it out loud. Read the direction of that check carefully, because the reference now spells it out: a found construct settles the matter, and finding none settles nothing whatever, since one file cannot show an engine that lives behind a helper, arrives with the row, or is bound while the process runs. A run the check does not stop is a run it had no grounds to stop, never one it cleared, and the sentence beside the refusal says so instead of implying an inspection nobody made. The other half of this entry is a negative result worth more than the feature it replaces. A bounded in-process route to the same evidence was built and then attacked: closed sockets, denied child processes, a read-only database handle under a step allowance, capped processor time and file growth. One adversarial pass walked out of it seven times, through classes rather than tricks, and the worse discovery was that its own report kept describing those bounds as holding while the process was already outside them, because the report was written beside the locks rather than measured after them. So the reference records why the route does not exist, the opening sentence of that section finally has evidence under it, and the guide keeps sending this work to a boundary the operating system enforces rather than one a language pretends to.

Weighed on a tree carrying this change alone, after the merge that brought the static reading in, and against the state the preceding entry recorded. The stage reference takes nearly all of the growth. The single largest file is untouched, so the budget that watches one file rising on its own keeps the figure it holds and is restated nowhere here.
