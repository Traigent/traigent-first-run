# 0146 - a disclaimed file is not one this run created

follows: 0145
follows-resident-measured: 54_163
follows-total-measured: 574_522
follows-document-measured: 177_420
resident-ceiling: 54_300
resident-measured: 54_237
total-ceiling: 574_767
total-measured: 574_710
document-ceiling: 177_639
document-measured: 177_483

One hundred and eighty-eight bytes across five documents, and no ceiling moves.
component-creation.md declares a component `generated` when this run created it or relies on it in
the customer's place, and names a customer's disclaimer of a file already in their project as the
second route. Several
passages decided a disclosure or a board mark by the first route alone, so a disclaimed evaluator
adopted as found fell through them - and two of them could mark it as the customer's own.

SKILL.md and GUIDE.md, 37 bytes each: both said a substitute this run created carries no board
mark, beside a `✅` defined as a real component found and validated. A disclaimed file that reads
cleanly and calibrates is found in the project and validated, so nothing kept it off `✅`. Both
sentences now add "or relies on in the customer's place", together, because GUIDE.md restates the
SKILL.md rule and changing one would leave the two statements of it disagreeing. Which mark a
component earns is not changed: a relied-on file was already a substitute by the origin rule.

component-creation.md, 38 bytes: its provenance classes are what the board is sorted by, and `real`
was "project/customer material that exists independently of the walkthrough" - which a disclaimed
file is, so it could be classed `real` before any mark was chosen. `real` is now the customer's own
material, and `demo` adds "or relied on in the customer's place": `demo` is the class the board
already renders as a walkthrough substitute beside the real-world gap, and `limited` and `missing`
do not fit, since one keeps the file as the customer's real anchor and the other rebuilds it.

evaluation-and-dataset.md, 13 bytes: the routing paragraph said neither generated ceiling is a
repair because "this run created the component on purpose"; it now reads "created or relies on".
run-safety.md, 63 bytes: the pre-spend card fired on material this run generated or repaired, so
the gap bullet never reached a disclaimed evaluator. The trigger now adds the relied-on route; the
gap bullet points at that trigger; its example gap reads "no grading method of theirs existed";
what fills the gap is "what stands in for it", not "what this run put there"; and its drop rule is
keyed to the second trigger alone, so a card both triggers raised keeps the bullet. The close's
"what this run created or repaired" line and the paragraph naming each pillar that needs a real
replacement now include the relied-on route. None restates the origin rule, whose one home stays
component-creation.md.

Every edited document was read end to end. RESIDENT grows 74 inside the 137 bytes 0142 left,
leaving 63; DOCUMENT, still run-safety.md, grows 63 inside 219, leaving 156; TOTAL grows 188
inside 245, leaving 57. The rewrap of the edited run-safety.md paragraphs moved no byte.
