# 0084 - the answers are read before the verdict

follows: 0083
follows-resident-measured: 105_335
follows-total-measured: 481_003
resident-ceiling: 107_100
resident-measured: 106_624
total-ceiling: 484_600
total-measured: 484_141

The row-by-row read of an answer key was ordered only on the branch where calibration is postponed, so a project that calibrated successfully was never asked for it and nothing anywhere noticed that it had not happened. A corpus whose expected answers were shuffled between its own subjects then cleared every structural gate this package owns - valid answers, unique keys, disjoint halves, four difficulty tiers, sixty of sixty executing - and collected the warmest verdict the card can issue over material where no configuration can beat another. Moving that instruction up one paragraph, ahead of the branch, is what makes the requirement bind whichever way the calibration decision goes, and the sentence beside it now states plainly that arithmetic over a file cannot settle whether an answer belongs to its own question. The reference that owns this stage gains the other half: what a clean read buys, which is the release of a hold on the warmest two verdicts rather than any credit, plus how much reading it takes to earn that release and the fact that a half-finished read earns none of it. Three shorter passages follow the same seam. The stage-4 re-score and the closing score now carry the same read, because the hold applies to every card and not only the opening one, and the paragraph that owns the release now says what the opening pass on a large corpus does: it names its count, releases nothing, and points at the re-score where the drawn rows can be covered - the neighbouring paragraph had mandated exactly that partial pass while the release rule refused it. One further clause records that the release is taken on the reviewer's word, since entries are counted and never matched to rows. The last of these bytes goes to the glossary, which is the document the flow sends the assistant to when a customer asks what a line on the card means, and which defined a band purely as the range its number falls in. A mechanism that holds a band without moving the score makes that definition wrong on the very card the reader is holding, so the entry now says a shown band can sit below the one its number names, names the two things that hold it, and says neither is a cap and neither stops the run. A run postponing its scoring probe now hears the postponed probe named back to it instead of an all-clear, and a run whose probe was refused by the execution-scope rule hears the containment review named instead - because telling that run to finish the probe is telling it to do what this package forbids, and the card must never be the one voice arguing for the unsafe route.

Measured against the ledger state 0083 left behind, on a working tree carrying only this change, with three assistant-facing documents edited and nothing else touched: the flow document, the evaluation reference, and the glossary. The largest single document is untouched by all of it, so the ceiling that governs one file stepping alone is restated nowhere and keeps the number it already had.
