# 0085 - a forbidden check gets a route and a lock

follows: 0084
follows-resident-measured: 106_333
follows-total-measured: 482_408
resident-ceiling: 107_000
resident-measured: 106_857
total-ceiling: 486_000
total-measured: 485_629

The rule against grading with a scorer that runs what the model wrote was carried by prose and nothing else: the helper's only refusal asked whether importing was intended, and a query-log project measured forty-four points and a band lower for obeying the sentence than for ignoring it. Two things were missing and each made the other unaffordable. Enforcement was missing, so the sentence bound only a reader who happened to find it; and any route to the same evidence was missing, so enforcing it would have walled off a whole class of real projects at a stop rather than a step. Both arrive together here. The helper now reads the scorer's reachable path before importing a line of it and refuses two verdicts - a call into an engine, and a path it could not follow to the end - which is why the reference that owns this stage says the refusal is executed rather than described, and says that a scorer nobody can classify is turned away instead of waved through. Beside it the same reference bounds the one narrow way through: identical probes with the socket layer shut, child processes denied, the database handle opened without write permission under a step allowance, and processor time and file growth held down, with the child reporting back what it actually locked rather than the flag asserting it. A third correction rides with them and is the sharpest of the set, because the refusal was saying something untrue. The stop covered two separate moments under one sentence and justified both with a reason that holds for only the later one. Checking a scorer runs the four pairs the assistant wrote into its own matrix, so nothing the model produced runs there at all, and an owner asking why a query scorer cannot be checked on five known pairs when every other kind of scorer can was owed a better answer than the one on offer. What is genuinely unbounded at that step is the store the scorer opens, which is a question about the target rather than about who wrote the statements; the later moment, where the model composes the query and the scorer runs it, is the one the old reason described and it stays shut under every flag. Each is now refused on the reason that is true of it, and the registry of settled wordings gains that pair so the collapsed version cannot come back. The flow document gains three clauses only, because the depth belongs where the stage lives: try the bounded way before declaring the refusal, the stop it already ordered now names the one exception to itself, and that stop says which of the two moments it is about.

Weighed on a tree carrying this change alone, against the state the preceding entry recorded. The stage reference takes most of the growth and the flow document takes three sentences of it. The single largest file is untouched, so the budget that watches one file rising on its own keeps the figure it already holds and is restated nowhere here.
