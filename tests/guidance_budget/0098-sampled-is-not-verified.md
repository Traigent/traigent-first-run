# 0098 - sampled is not verified

follows: 0094
follows-resident-measured: 106_368
follows-total-measured: 498_243
follows-document-measured: 136_303
resident-ceiling: 107_600
resident-measured: 107_053
total-ceiling: 502_600
total-measured: 501_133
document-ceiling: 137_550
document-measured: 136_303

**This raises RESIDENT by 1_100 bytes and TOTAL by 4_300, on the owner's decisions recorded in
traigent-first-run#441 and #439.** DOCUMENT does not move; no single file grew.

The hold on the top two bands used to ask for a read of every answer the run is graded against, and
on any corpus larger than the drawn subset that could never be delivered - the review covers the 28
rows drawn while the score reads all 4,812, so a customer doing exactly what the guide asks was
told their answers were unread, permanently, on this guide's own worked example. The owner replaced
the premise rather than the denominator: five rows, judged against the method that will grade them,
and the rest assumed to be like them. A full input-versus-expected comparison is refused, not
skipped, because for a retrieval task whose input is a PDF nobody should be put through it to start
a first run.

That trade only stays honest if the customer is told about it, so the bytes buy disclosure and not
rules. 685 of them are RESIDENT, in the two places the flow reaches the reader: the opening gate now
orders the sample to be explained in the turn that prints the first card, and the close now orders
the production-ready sentence. The remaining 2_205 are the depth those two mandates point at, and the glossary entry that
defines the held band, which described a row-by-row read the guide no longer performs.
`references/evaluation-and-dataset.md` gains why five rows and not the file, what a released hold
does and does not establish, and the sentence a customer hears - checked a few of your answers
against the method that grades them, they held, the rest is assumed, look yourself if the key was
not assembled carefully - plus the disclosure that where this run wrote the rows or the method, the
sample was taken through its own work. `references/run-safety.md` gains the paragraph #439 asked
for: every generated pillar made real or checked by a person is what production ready means, said
beside what the run was worth without them - portal results, next steps, the shape of easy against
hard, and a split held out of the search - because a generated corpus is not a full-power run and
is not a worthless one either.

Why a raise and not a prune: the passages this replaces were shorter than what replaces them,
which is the direction a disclosure goes. Removing the unliftable coverage rule gave back roughly
140 bytes and the de-duplication of the sampled sentence into one home gave back none, so the whole
increment is prose nobody can act on if it is compressed further. Both ceilings are sized so the room left
under them is at least one typical raise of that budget, which is what this ledger's own headroom
note asks for: 547 spare RESIDENT against a 500-byte median raise, and 1_467 spare TOTAL against a
1_400-byte one. Sizing the package for a batch of branches is still the owner's decision and not
this one's; these numbers only refuse to hand the next author a wall.

On merging, and read this before trusting a figure above. Every number here is measured on this
branch, which is trunk plus this change and nothing else, so `follows:` names 0094 - the newest
entry that exists in this tree, and the only one an entry is allowed to follow. Three entries land
first: #434's 0095 and 0096, which take TOTAL to 499_461, and #443's renumbered 0097, which adds 12.
This change is worth 2_890 TOTAL and 685 RESIDENT over trunk, so the merged package should measure
about 502_363 and 107_056 - both inside the ceilings above, which were sized for a typical raise
rather than for this arithmetic and happen to cover it. Re-point this entry at 0097, restate
`follows-*-measured` from it, and re-measure; if a fourth branch lands in between, the same three
steps apply to whatever is newest. What may not happen is keeping these figures beside a new
predecessor, which reads to the suite as a prune nobody performed.
