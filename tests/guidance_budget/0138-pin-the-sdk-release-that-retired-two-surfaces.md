# 0138 - pin the SDK release that retired two surfaces

follows: 0137
follows-total-measured: 567_849
total-ceiling: 567_900
total-measured: 567_654

The tested SDK moves from 0.26.0 to 0.27.0, the newest release on the index. Reading the
release against every import and dotted name the guide names found two casualties: the
`traigent.config_generator.recommendations` module behind the knob-recommendation row no
longer ships, and the named selection presets behind the presets warning are deleted, so
both decorator arguments now raise `TypeError` instead of quietly swapping the objective.
The row leaves the navigation table because it pointed at a module that cannot import;
the warning keeps its mandate and states the measured refusal in place of a rationale
about a preset that no longer exists. Version strings in the retention and loader notes
follow the pin. No stage, display mode, or file is added; the package shrinks by 195 bytes.
