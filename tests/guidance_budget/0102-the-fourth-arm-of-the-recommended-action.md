# 0102 - the fourth arm of the recommended action

follows: 0099
follows-resident-measured: 106_368
follows-total-measured: 498_278
follows-document-measured: 136_303
resident-ceiling: 106_500
resident-measured: 106_386
total-ceiling: 498_300
total-measured: 498_296
document-ceiling: 137_550
document-measured: 136_303

`recommended_action` grew a fourth arm, and SKILL.md's one-sentence account of the field named
three. A run may now owe something that caps nothing - the answer-key hold is the first, and its
remedy arrives in the field a consumer routes on rather than only in the card's prose
(traigent-first-run#396) - so the sentence as it stood described a scorer that no longer exists,
in the document that tells the assistant how to read that field. The condition ids behind it stay
internal, so no route, no branch and no remedy word is added to the flow: the assistant already
routes `review-answer-key`, which is the slug the cap table carries for the same question one axis
over.

**No ceiling moves, and the sentence pays for most of itself.** The clause
`otherwise an ask that caps nothing` is 36 bytes and only 21 were usable, so the same sentence
gives 18 back: `when a cap blocks` is deleted from the first arm, where it restated the word
`blocking` beside it. Net 18, and the four arms now read in one parallel form - blocking remedy,
asking one, ask that caps nothing, `proceed` - which is what the arm order is. The prune is inside
the sentence being extended, so no other rule, document or decision is touched to pay for this one.

This entry was measured three times, on three different trunks, and the only reason it is worth
recording is what moved in between. Against `c2e9c8bf` the corpus was 498_243 with 57 bytes of gap
and the unpruned 36-byte clause fitted with 21 to spare. `0099` then spent 35 of them, and the same
clause no longer fitted at all - 498_314 against a 498_300 ceiling, over by 14 - without a single
byte of this change having altered. A figure carried from an earlier trunk would have read as
comfortable and been wrong, which is exactly what `follows-*-measured` exists to catch.

**TOTAL is now effectively spent: 3 usable bytes under a strict `<`.** RESIDENT has 113 and
DOCUMENT 1_246, so the wall is TOTAL alone. Nothing here raises it, because this change does not
need it: an entry that banks a raise it has no use for hands the next author a number nobody
weighed, which is what this ledger's own root entry was written about.

The number is 0102 by assignment, and `follows:` names 0099 rather than 0101 for the reason this
ledger gives everywhere else: an entry may only follow one that exists in its own tree, and 0099 is
the newest that does. The numbering gap is not a missing entry - trunk itself reads 0094 then 0099.
0101 could not be bound to in any case, because no branch has yet published an entry under that
number: the two branches ahead of this one both currently number theirs 0100, so a tree holding both
cannot be walked at all, and the figures the future 0101 records today were measured without its own
predecessor in front of it. Binding to them now would be stale by construction, which is what
`follows-*-measured` exists to refuse.

WHAT THE MERGE ACTUALLY MEASURES, so the re-point is arithmetic and not a forecast. Merged with the
three branches ahead of this one - #442, which carries no ledger entry, #443 and #459 - the corpus
measures 498_239 TOTAL and 106_368 RESIDENT without this change, and **498_257 and 106_386** with
it. This change is worth the same +18 there as it is here, because both branches ahead of it are
prunes that touch other sentences: #443 frees 27 and lowers its ceiling by exactly that, #459 frees
12, and the two are disjoint - trunk's 498_278 less 39 is the 498_239 measured, not a figure derived
from one. Against #443's 498_273 ceiling that leaves 15 usable bytes, and against #459's 498_288,
30; this change fits under either, so the order those two land in does not decide whether it fits.
Whoever integrates last re-points this entry at the newest entry that then exists, restates the
bindings from it, and re-measures - the three steps this entry has now been through three times.