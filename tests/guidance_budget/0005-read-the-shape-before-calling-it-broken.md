# 0005 - an unreadable file gets read before it is called broken

follows: 0004
resident-ceiling: 62_750
resident-measured: 62_650
total-ceiling: 234_500
total-measured: 234_004

#144 adds a route nobody had: a dataset whose shape no row matched is not a
dataset that is missing and not one that is wrong, so sending its owner into
the creation matrix asks them to rebuild a file that may be perfectly good.
The new bullet has to say READ - re-map the field paths, or convert the format -
and it has to say it in SKILL.md, because the creation matrix is what an
assistant reaches for by default and only a bullet before it can redirect that.
The depth behind it, four phrases the suite pins, lands in
`evaluation-and-dataset.md` where the dataset stage lives.

The two lower-ceiling bullets also stop hedging and start naming what lifts
them: both emit `get-data` and neither mentioned data, which is a routing
statement disagreeing with the action it emits.

#144 declared 230_070 and its own author noted it was estimated from an
increment. Here it measures 234_004, and resident 62_650. Nothing about that is
#144's fault - four other branches raised prose in the same two files first.
