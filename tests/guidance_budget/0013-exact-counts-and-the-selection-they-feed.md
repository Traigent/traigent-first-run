# 0013 - exact counts, and the selection rule they feed

follows: 0012
total-ceiling: 273_800
total-measured: 273_360

#174 replaces "roughly" with two exact sizes - baseline 12, enhanced 48 - and
asserts both beside the spaces that produce them, so the number the assistant
reads and the number it runs cannot drift apart without a red test. The bytes
are not the counts; they are what the counts made sayable. A baseline of three
models by one further two-valued knob could only rank one lever, so the branch
adds a second non-model axis, and the enhanced space becomes three models by
four binary behaviour knobs. Each of those decisions needs its reason written
where the assistant meets it: why two values is FULL breadth for a categorical
knob rather than half, why a knob declared with exactly one value scores ten
rather than zero, why `self_check` and `reflect` were one knob under two names,
and why one task-selected temperature is fixed in both spaces instead of swept in one.

The larger part of the rise is that #174 and #169 both landed on the same
section from opposite ends and both survive. #169 owns WHICH of a customer's
knobs fill the slots, read from what the baseline actually varied; #174 owns
which TWO VALUES each slot then carries, read from the lowest-scoring
combination. #174's own text names the other half - "decided by the
baseline-evidence selection above" - so it was written to sit after #169 rather
than instead of it, and merging them is addition, not choice.

273_360, measured on the merge of #168 and #174 into the twenty-three before
them. Neither branch could have measured this: #174 declared 242_250 against a
base without #169, #157, #140 or #171 in it. RESIDENT moves 21 bytes, to
64_344, still inside the 64_500 bought by 0011, because every one of these
sentences lands in the two references a run loads and leaves.
