# 0133 - install in the project and attribute the copied check

follows: 0132
follows-resident-measured: 52_476
follows-total-measured: 549_527
follows-document-measured: 152_371
resident-ceiling: 54_400
resident-measured: 54_368
total-ceiling: 571_500
total-measured: 571_443
document-ceiling: 171_500
document-measured: 171_314

The installation sequence now leaves the SDK in the customer's existing environment
or, when none exists, a persistent project .venv. The fallback remains an explicit
choice. Environment selection reads metadata through a trusted host interpreter;
an unfamiliar runtime or package inventory remains visible as unverified. The
installation approval names each resolved addition and exact installed-version
change. Applying its bound artifact plan cannot silently resolve a different stack
or execute the customer's startup hooks before that approval.

The execution-evaluator exception uses a copy with one engine-target AST change
against the customer's read-only or duplicate target. Comparison evaluators and
LLM judges retain their direct route. Copy and target provenance stay in local run
evidence; the card attributes the supplied result without authenticating its
producer. The scope declaration still describes execution of the original evaluator,
so supplied credit does not erase that disclosure. Existing customer code remains
outside the copied route's threat model.

This is the integrated measurement after the refusal and supplied-result scoring
stack. It replaces the concurrent installation branch's unmerged ledger entries
under the README's collision rule. Historical entries through 0132 are preserved;
no earlier branch's figures are presented as measurements of this package. Stage
detail belongs in run-safety, and the entry flow and handoff dispatch there. The
closing text reports only the setup and measurements this run actually established.
