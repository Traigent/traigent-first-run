# 0083 - the resident file says where to read

follows: 0082
follows-resident-measured: 99_652
follows-total-measured: 475_250
resident-ceiling: 105_800
resident-measured: 105_335
total-ceiling: 481_500
total-measured: 481_003

A blinded run held this whole file in context, chose the hand-rolled local grid it prescribes, and
then wrote its script having grepped only the headers of the longest bundled reference - stopping
well above the line where that grid is actually specified. What came back had no spend ledger, no
kept record and no closing card. Nothing was absent from the package. The resident file simply
never said which of five references, or which of their thirty-eight second-level sections,
answered the step under way, and thirty-two of those sections were named nowhere in it at all.
These bytes buy the sentence that says so. At each point where the flow prescribes work there is
now one destination that must be read, and beside it a condition per genuine branch, each settled
from evidence the run already holds rather than from a guess about its situation. Paying for the
conditional half here rather than pushing it into a reference is the whole argument: it is the
only mechanism in this package that lets a correct run decline to open a document, and both
transcripts of that campaign were dominated by guidance rather than by the customer's own
material. Thirty-two pointers and the sentence that says a dispatch governs its stage replace no
prose and cost 5_683 bytes, which is a real price on the part of the corpus that competes with the
project from the first turn, so it is recorded here plainly instead of argued down. The document
ceiling is untouched: all but seventy bytes land in SKILL.md, the seventy complete one reference's
own index of itself, and the largest single reference is unchanged.
