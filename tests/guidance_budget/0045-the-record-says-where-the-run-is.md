# 0045 - the record says where the run is

follows: 0044
resident-ceiling: 82_795
resident-measured: 82_447
total-ceiling: 373_011
total-measured: 372_663

The run record now opens with a stage-status block, and SKILL.md gains the
cadence that keeps it current plus the resume rule for a matching record.
Before this, only the mismatch branch was written down: a fresh session after
context loss, following the flow literally, would restart a half-finished run -
re-running the opening gate whose recorded score it must never overwrite, and
asking the user to pay again for stages already approved and spent. Fourteen
template lines and one resident paragraph buy a persisted you-are-here that any
later session can continue from and support can quote, and they replace
nothing, because the matching branch had no prior statement anywhere in the
guidance.

Both figures are `assistant_facing_documents()` measurements: 82_447 resident,
372_663 total, re-taken after review wording fixes landed ("neither marked done
nor skipped", the skipped stage keeping its checkbox), after the guidance this
entry follows grew underneath it on trunk, and after GUIDE.md's resumed-run
paragraph gained one sentence restating the resume conclusion locally, so
GUIDE.md's unconditional opening mandate no longer reads as the last word for
a resumed session. Review also closed two safety boundaries: a project-owned
record never authorizes a new external call, and matching paths never prove a
paid comparison's inputs stayed unchanged. The run record now captures those
comparison invariants while verifying the intentionally changed enhanced space
separately; drift archives the old run and starts a fresh one instead of
silently reusing paid evidence. Status remains a resume hint rather than proof:
each fresh session independently re-verifies the safety gates its next action
needs. Those resident bytes are why the ceiling rises past the
first measurement instead of standing; the template counts only toward the
total. Both ceilings move with the re-measurement, keeping the 348-byte margin
this entry was first written with.
