# 0101 - the refused card explains itself

follows: 0100
follows-resident-measured: 106_371
follows-total-measured: 498_538
follows-document-measured: 136_303
resident-ceiling: 106_500
resident-measured: 106_371
total-ceiling: 499_500
total-measured: 499_444
document-ceiling: 137_550
document-measured: 136_303

**This raises the TOTAL ceiling to 499_500 against a measured 499_444, and it is the only ceiling
that moves.** It is also the smallest raise this entry has asked for, and the paragraph it buys is
now 905 bytes rather than the 1_580 an earlier draft of it took.

What was missing was not a rule but a disclosure. A project whose evaluator reaches a code or SQL
engine has its calibration refused, earns nothing for that check, never measures probe spread, and
carries a 45 ceiling - and no assistant-facing document said any of that would happen, so the first
the customer heard of it was the card. Every part of that outcome follows from a decision this
guide makes on their behalf, about an evaluator nothing here says is wrong. The reference that
argues the decision now states its consequences, including the half that is not free: whether the
unmade check is charged depends on what preflight's walk found, and a run resting on the
declaration alone pays, because finding no engine settles nothing.

One home, and it is this one. `SKILL.md` carries the mandate - section 4's "Before calibration,
apply `references/run-safety.md`'s execution-evaluator scope gate" - and the wording mandate for
this cap, and it is the card that states the customer's route. The reference points at each rather
than restating it; the depth behind a stage belongs to the reference, the flow and the mandates do
not.

**On paying for it out of a prune rather than a ceiling.** That was the instruction and it is the
right instruction: a reader-load audit found this branch and one sibling carrying almost all of the
corpus's growth, and a guide the assistant cannot follow faithfully is worse than a guide that
explains less. I took the prune I can defend, inside my own change: the disclosure paragraph was
rewritten from 1_580 bytes to 905, a 437-byte saving on the tree, by moving its reasoning here -
which is what this ledger is for - and by pointing at the card's route instead of restating it.

**It is not enough, and the remainder is stated rather than hidden.** After the prune this branch
still costs 1_166 bytes against 22 of headroom on trunk, so 1_200 of ceiling is unpaid. The two
candidates the audit named did not yield it to me, and I would rather say so than cut something I
cannot defend:

* The entry point does restate rules `SKILL.md` owns, but three of those restatements are pinned to
  `GUIDE.md` SPECIFICALLY by `test_user_owned_baseline_is_not_padded_to_generated_row_target`, and
  "connected managed optimization" appears in none of the other three documents
  `test_public_promises_match_the_local_then_connected_sequence` reads. Cutting them is not a prune;
  it reverses settled decisions from other changes, or moves the bytes into the resident half of the
  same corpus.
* The largest reference is 136 KB and the paragraphs a marker scan surfaces are live operating
  instruction, code comments carrying measured findings, and the rationale behind three ordering
  rules - not rejected-design records. There may well be 11_000 bytes of archaeology in it; I could
  not locate them by search, and cutting a reference on an unverified estimate is the failure this
  branch has already been corrected for four times.

So the raise is the honest remainder, minimised: 1_200 rather than the 1_800 this entry held before
the prune. Whether the corpus should give the bytes back somewhere else, and where, is a decision
about the package rather than about this paragraph, and it is the owner's.
