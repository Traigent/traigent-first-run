# 0143 - route A lettering, bound and integrity

follows: 0142
follows-total-measured: 572_500
follows-document-measured: 175_426
total-ceiling: 574_800
total-measured: 574_555
document-ceiling: 177_700
document-measured: 177_481

Entry 0142's route was reviewed after it merged, with the gate executed rather than read.
Four things it left open close here. The Setup sequence's step 7 mapped letters to routes,
which is wrong the moment a server target drops route A and the pasted route becomes A; step 7
now names the routes by what they do, and a skip reply runs nothing. Two sentences still said
.env receives only the provider key; both defer to the rule that owns .env writes. The 256 MB
bound named no measurement and a symlink defeated it: the file is resolved with realpath and
measured with stat -L, must lie inside the resolved project root, its sidecars are sought beside
the resolved file, and the question shows the resolved path and size the customer is agreeing to
copy - or says why route A is not offered. A copy torn by a writer with no visible sidecar would
have read as the evaluator failing; SQLite's integrity check now runs on the run's own copy
through the chosen interpreter's stdlib, so no CLI is needed, and its output is described as it
is - ok, or damage named by page, table, index and row, never a stored value; a torn copy is
retaken or the question is re-put without route A. DuckDB has no such check and says so: the
customer's word that nothing writes the file is the guarantee, and a failure there is reported as
an unverified copy. 2,055 bytes, the total ceiling rises by 2,000, and run-safety.md rises by
2,000.
