# 0077 - the build half is settled against the tree

follows: 0076
follows-total-measured: 462_697
total-ceiling: 467_600
total-measured: 467_226

A blinded fixture re-run spent these bytes before anyone wrote them down. Its
dataset was rebuilt underneath it, so the recorded invariants no longer matched
and the run correctly refused to resume: it retired its record, copied a fresh
template, and restarted at stage one for a new opening score. What it carried
across that boundary was the previous run's reading of the agent, byte for byte,
sha256 unchanged. Half of that document was refused on arrival by the coordinate
check every parameter already had to pass. The other half was printed onto the
customer's card as an observation about their program, and one of its sentences
said the program takes one turn and stops over a program that loops forever.

The first attempt at this entry bought bytes for a coordinate on every settled
check. Review found that answering a question about meaning from a fact about
line numbers is the same defect one layer up: a hand-verified citation and an
arbitrary in-range one produce identical output, so the lying document reopened
by adding one field. The bytes here buy the replacement, which is a reader that
settles what the source can settle and says plainly where nothing can.

Two of the four checks can be contradicted by the tree the module already parses.
A loop node in the selected callable refutes a claim that the agent takes one
turn. A declared tool absent from the whole file refutes itself. And a loop whose
test is a literal constant and whose body has no break, no return and no raise
refutes a claim that there is a stop condition to point at, which matters because
that is the half of the answer pricing the risk.

That last rule cost a second correction, and the bytes explaining it are the
reason this entry grew. A first version refused on the body alone, reasoning that
a loop nobody breaks out of never stops. True premise, false conclusion: the
usual way such a loop ends is its test becoming false, so it refused a counter
winding down to zero and a flag set inside the body, printed a warning about
unbounded cost over code that plainly finishes, and left its author no true
document to write. The guidance now names the counter and the flag as bounds to
record, and says which single shape is refused and why only that one can be.

None of the three confirms anything, and both the paragraph and the rendered line
say so, because a reader who takes silence for proof has been misled by the
document rather than by the scorer. Neither read leaves the selected function's
own body either, so an agent handing its loop to a helper clears both while never
returning, and the line beside each answer now states how far the check reached
rather than that a check happened.

The direction is the expensive part to explain and the part that had to be
written down. An equality between the declaration and the tree was implemented
and rejected: this check asks whether the agent ends, which is a question about
behaviour, while the tree answers a question about syntax. A program ends badly
without holding any loop of its own, by delegating to a helper that spins, by
recursion, or by handing the work to a comprehension, a generator or a mapping
call. Comparing the two answers for equality refused every author who read those
correctly, told them they had read the wrong program, and instructed them not to
adjust their answer - leaving exactly two acceptable documents for a program that
never finishes, one false and one disclaiming a judgement its author had really
made. So the guidance now names the shapes to record and promises they will be
accepted with no matching node beneath them.

The rest goes to the two questions nothing decides. Whether instructions carry
worked examples, and whether anything fixes the form of an answer, are not
settled by any walk of a syntax tree. Those checks record where somebody looked
and what they saw, and both the guidance and the rendered line beside them now
say that rather than letting four answers read as equally examined. A reader
comparing a checked answer with an unchecked one gets the difference where they
meet it, which is the confusion the originating report opened on.
