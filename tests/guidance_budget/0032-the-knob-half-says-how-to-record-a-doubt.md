# 0032 - the knob half says how to record a doubt

follows: 0031
follows-total-measured: 351_095
total-ceiling: 351_600
total-measured: 351_485

Two sentences told a reader to write down a parameter they were unsure of instead
of dropping it, and neither said what that record looks like. The only shape
either one showed was the `determined` answer, which belongs to the build checks;
written among the knobs it hit a closed-field refusal on the very first scoring
call of a guided walkthrough, before any opening card existed. The observed
recovery was trial and error, ending with the doubtful parameter deleted - the
precise loss the instruction was written to prevent.

What these bytes buy is the missing half of an instruction that was already
here. The knob reader has carried a third answer since it was written: a
parameter recorded with its citation and no established options or bounds is read
as seen-but-unmeasured, earns no dimension, costs nothing, and comes back with
the sentence explaining why it counts for nothing. Nothing in the reference
pointed at that spelling, so the state existed and was unreachable by anyone
following the page.

Stated in the reference and not in the flow, because it is the shape of one
input to one stage rather than a rule about what a run must do. The flow keeps
its mandate untouched; this says which of the document's two halves the
`determined` answer belongs to, beside the promise that sends a reader looking
for it. Widening the schema was the alternative and was refused: a knob that
establishes no range contributes nothing a search can vary, so a second spelling
would be two ways to write one state, which is the drift these tables exist to
delete rather than relocate.

351_485, measured with `assistant_facing_documents()` after the paragraph landed.
Resident is unchanged at 76_711 and that ceiling does not move - the flow already
mandates the read, and what was missing was the reference's own worked shape.
