# 0086 - a query answer gets a ruler that reads queries

follows: 0085
follows-total-measured: 487_639
total-ceiling: 490_000
total-measured: 489_541

Measured on an external bank: an obedient text-to-SQL project, calibrated and in scope, reads 88
and stops there. Its output kind fitted two rulers. One was the executing scorer that ends this
guide at the safety reference, and the other was a blend priced at half reproducibility because
nothing in the scorer can see which checks went into it. So the only destination this table
offered a query answer was a ruler charged as though it might phone a model, and the top band was
reachable only by typing a word the evaluator did not support. A third destination closes that,
and these bytes are what it costs to state honestly.

The row itself is the cheap part. Two paragraphs beside it are not, and both pay for something the
row cannot carry. The first bounds what a parse settles without a catalog, because this document
already mandates that an author write a semantically equivalent probe and then blocks the run when
their evaluator scores it low: told only that a ruler compares structure, an author has no way to
know that reordered conjuncts pass and that a bare column name never matches a qualified one, so
the probe they invent decides whether they are stopped. The second paragraph says where the credit
comes from. Fit for this ruler is settled against the evaluator file, not against the word, so a
reader has to learn here that a fine comparator of their own grades correctly and is paid nothing,
and which word to declare instead. Left out, that arrives as an unexplained refusal on a card,
which is the failure mode this repository keeps filing against itself.

Nothing in the entry documents moved, so the resident figure is untouched and unbound here. The
per-file ceiling is untouched as well: this reference grows to 83_296 bytes against a largest
document of 128_943, so it comes nowhere near stepping alone. The comparator that the route hands
the customer is a Python file under `assets/`, which no ceiling here counts, so what is paid for
is only the prose that tells somebody when to reach for it and what it will refuse to tell them.
