# 0135 - route approval and scope ownership

follows: 0134
follows-resident-measured: 54_360
follows-total-measured: 571_514
follows-document-measured: 171_231
resident-ceiling: 54_346
resident-measured: 54_331
total-ceiling: 573_700
total-measured: 573_551
document-ceiling: 173_500
document-measured: 173_386

Environment planning now names the existing-project route before presenting
its approval card. The two creation routes dispatch to their existing approval
instructions, so selecting the fallback cannot accidentally acquire a second
confirmation step. Every route continues through the same package plan and
application mechanism. This clarification slightly grows the largest reference.

The same environment section now names the existing local-wheel planning route
for private or offline package sources and distinguishes an unsupported version
declaration from authenticated runtime identity. The Windows lookup retains the
no-bytecode flag. These are remedies and consistent invocations of existing
controls, not additional setup stages.

A failed dependency plan now has a customer-facing route instead of silently
substituting a different dependency stack. The fallback's compatibility warning
and the paid card's direct-provider accounting limit state consequences the
existing preservation and spend controls do not establish on their own.

The opening component procedure replaces repeated refusal arithmetic with a
direct scope link and points the non-Python advisory card to the existing adapter
boundary. Those dispatches keep the actual decisions in their owning sections.
Resident guidance shrinks by narrowing its closing instruction to avoiding a repeated
environment choice, while permitting the recorded compatibility facts. Its ceiling
shrinks with it. The published 0134 record remains intact; this
single follow-up entry measures the combined corrections, with modest headroom
above the total and largest document rather than granting an unlimited reserve.

Peer-review refinements forward the detector’s declared-version skip text,
explain relocated startup caches, and recommend a compatible environment
without suggesting a dependency downgrade. The same entry is re-measured
for the final version of this unmerged PR; no historical record is changed.
