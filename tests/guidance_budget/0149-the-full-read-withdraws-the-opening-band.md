# 0149 - the full read withdraws the opening band

follows: 0148
follows-total-measured: 575_903
total-ceiling: 576_177
total-measured: 576_173

Two hundred and seventy bytes, one sentence, in the row question of evaluation-and-dataset.md.
An opening card can read `EXCELLENT` off five sampled rows, and the section-4 read of every
selected row can then raise `dataset-unsound-expected-outputs` on rows the sample missed, which a
five-row draw does 42 to 59 percent of the time at the cap's share. The customer was told about the
rows and never that the band they had been shown no longer held. The sentence tells them, in words
and naming no band in its place, so the old band is never paired with a new one, and only where
the recorded opening band was one of the top two and the section-4 gate result carries that cap.
It adds no number, no record field and no question, and it replaces nothing.

Known limit: a mixed review whose out-of-run `no` verdicts carry the cap while the in-run ones stay
under the share still fires it, which only a read wider than the selected rows can reach.

evaluation-and-dataset.md, SKILL.md and glossary.md were read end to end: the first because it is
the home, the other two because each states a part of the decision that only the opening score is
reported. component-creation.md was read at its opening procedure only; it points at the row read
and restates none of it. SKILL.md and GUIDE.md are unchanged, so RESIDENT is not declared, and
run-safety.md, still the largest document, is unchanged at 0148's 178_288 bytes, so DOCUMENT is
not declared either. The sentence 0148 added to evaluation-and-dataset.md, that the read alone
does not lift the band while the fit ask is open, agrees with this one: while that ask is open the
opening band is held at `WORKABLE`, and this sentence stays silent.

TOTAL rises by 270 bytes to four above the measurement, because only four bytes were left under
0148's ceiling. Four is the margin 0147 and 0148 kept, following the entries from 0102 to 0130.
This entry was first measured on top of 0147 and was re-pointed at 0148 and re-measured on the
rebased tree once 0148 landed.
