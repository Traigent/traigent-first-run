# 0136 - the request LiteLLM itself makes

follows: 0135
follows-resident-measured: 54_331
follows-total-measured: 573_706
follows-document-measured: 173_541
resident-ceiling: 54_346
resident-measured: 54_331
total-ceiling: 573_900
total-measured: 573_858
document-ceiling: 173_750
document-measured: 173_541

No ceiling moves. This entry records the tree after two clauses were added to
the accepted-route rules in component-creation.md, 152 bytes together, inside
the headroom 0135 left.

Rule 5 names LiteLLM's module-level request beside the provider client's own
request. The reader already credited a setting sent on `client.chat.completions
.create(model=model)` and refused the same setting sent on
`litellm.completion(model=model)`, which is the library this guide pins and
the call its own walkthrough wrapper makes. Measured on a public bank of
text-to-SQL demo projects whose agents call LiteLLM that way: every project
with an agent opened with the agent pillar at 0 and every setting reported as
one this read "could not follow to the request". The card's sentence is
rendered from the same constant the reference carries, so the wording changed
in one place and the suite holds the two together.

The range route says a `float(...)` around the read is followed and an
`int(...)` is not. The scorer already accepted the cast at the request
argument; the local that holds the read is the same route one line earlier,
and refusing it there sent an author to remove a cast the card accepted two
lines down.
