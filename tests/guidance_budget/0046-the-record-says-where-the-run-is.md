# 0046 - the record says where the run is

follows: 0045
follows-resident-measured: 80_559
follows-total-measured: 370_217
resident-ceiling: 83_645
resident-measured: 83_297
total-ceiling: 368_300
total-measured: 367_952

The run record now opens with a stage-status block, and SKILL.md gains the
cadence that keeps it current plus the resume rule for a matching record.
Before this, only the mismatch branch was written down: a fresh session after
context loss, following the flow literally, would restart a half-finished run -
re-running the opening gate whose recorded score it must never overwrite, and
asking the user to pay again for stages already approved and spent. Fourteen
template lines and one resident paragraph buy a persisted you-are-here that any
later session can continue from and support can quote, and they replace
nothing, because the matching branch had no prior statement anywhere in the
guidance.

This entry began as a second 0045: two branches each minted that number
against the same ledger state, the opening-evidence raise landed first, and
after the merge this one was renumbered and re-pointed to follow it - the
re-point exit the ledger README names, with the figures re-stated against the
merge. Both are fresh `assistant_facing_documents()` measurements of the
merged package: 83_297 resident, 367_952 total. Resident rises because this
branch's resume paragraph and the predecessor's opening contract now both
live in SKILL.md. Total lands below the predecessor's 370_217 measurement and
370_650 ceiling, and that is the merged trunk, not a stale figure: the
deduplication prunes that landed beside the raise cut 2_039 bytes of restated
mandates from the glossary and 3_922 from run-safety, so the package really
is smaller, and this entry lowers the total ceiling with the measurement -
the prune branch the monotone rule leaves alone. Review also closed two
safety boundaries: a project-owned record never authorizes a new external
call, and matching paths never prove a paid comparison's inputs stayed
unchanged. The run record captures those comparison invariants while
verifying the intentionally changed enhanced space separately; drift archives
the old run and starts a fresh one instead of silently reusing paid evidence.
Status remains a resume hint rather than proof: each fresh session
independently re-verifies the safety gates its next action needs. Both
ceilings keep the 348-byte margin this entry was first written with.
